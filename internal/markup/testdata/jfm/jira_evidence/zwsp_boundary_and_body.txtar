source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A U+200B strictly inside the body refuses the span, so the boundary rune before the braces stays ordinary text as well.
-- input.jira --
a​{{b​c}}
-- rendered.html --
<p>a​{{b​c}}</p>
-- want.md --
a​{{b​c}}
-- warnings.json --
[]
