source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A hexadecimal character reference is as opaque as a decimal one, so the space it encodes does not refuse the span.
-- input.jira --
{{&#x20;a}}
-- rendered.html --
<p><tt>&#x20;a</tt></p>
-- want.md --
`  a `
-- warnings.json --
[]
