source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A word rune before the opener blocks the citation exactly as it blocks an Effect Delimiter, so jiro stays silent.
-- input.jira --
{{a??x??}}
-- rendered.html --
<p><tt>a??x??</tt></p>
-- want.md --
`a??x??`
-- warnings.json --
[]
