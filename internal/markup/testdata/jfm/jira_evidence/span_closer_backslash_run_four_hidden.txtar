source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A run of four backslashes hides the closer and renders literally, being no pair; jiro refuses the span too and keeps all four backslashes.
-- input.jira --
{{a\\\\}}
-- rendered.html --
<p>{{a\\\\}}</p>
-- want.md --
{{a\\\\\\\\}}
-- warnings.json --
[]
