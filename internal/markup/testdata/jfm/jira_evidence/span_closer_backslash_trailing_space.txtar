source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The body ends in a space once the closer consumes the backslash, so the span is refused.
-- input.jira --
{{a \}}
-- rendered.html --
<p>{{a }}</p>
-- want.md --
{{a }}
-- warnings.json --
[]
