source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Edge spaces refuse the Monospace Span, and Jira substitutes an en dash in the literal text that remains.
-- input.jira --
{{ -- }}
-- rendered.html --
<p>{{ &#8211; }}</p>
-- want.md --
{{ -- }}
-- warnings.json --
[]
