source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The closer consumes the single backslash in front of it, and the body left behind is empty, so no span forms.
-- input.jira --
{{\}}
-- rendered.html --
<p>{{}}</p>
-- want.md --
{{}}
-- warnings.json --
[]
