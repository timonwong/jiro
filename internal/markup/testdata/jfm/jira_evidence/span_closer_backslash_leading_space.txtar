source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A leading space refuses the span even though the closer consumes the backslash in front of it.
-- input.jira --
{{ \}}
-- rendered.html --
<p>{{ }}</p>
-- want.md --
{{ }}
-- warnings.json --
[]
