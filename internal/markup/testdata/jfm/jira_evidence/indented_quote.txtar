source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: The same indent is skipped before `bq.`, so jiro converts the quote and keeps none of it.
-- input.jira --
 bq. x
-- rendered.html --
<blockquote><p>x</p></blockquote>
-- want.md --
> x
-- warnings.json --
[]
