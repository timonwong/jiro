source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A command-line flag has no space after the hyphens, so Jira leaves it alone.
-- input.jira --
{{--flag}}
-- rendered.html --
<p><tt>--flag</tt></p>
-- want.md --
`--flag`
-- warnings.json --
[]
