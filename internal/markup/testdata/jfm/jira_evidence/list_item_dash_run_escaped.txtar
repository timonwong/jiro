source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The same backslash keeps the rule off a list item's content start, so the item shows the dashes as its text.
-- input.jira --
* \----
-- rendered.html --
<ul>
	<li>&#45;---</li>
</ul>
-- want.md --
- \----
-- warnings.json --
[]
