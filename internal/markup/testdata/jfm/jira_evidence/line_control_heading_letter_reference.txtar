source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: Encoding the letter instead of the period protects the line too; the renderer encodes the period so the visible prefix stays searchable.
-- input.jira --
&#104;1. x
-- rendered.html --
<p>&#104;1. x</p>
-- want.md --
&#104;1. x
-- warnings.json --
[]
