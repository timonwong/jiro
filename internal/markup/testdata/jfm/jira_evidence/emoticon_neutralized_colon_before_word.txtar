source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The character reference stays literal wherever it stands, so jiro reads it back as text either way.
-- input.jira --
&#58;)y
-- rendered.html --
<p>&#58;)y</p>
-- want.md --
:)y
-- warnings.json --
[]
