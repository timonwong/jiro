source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A named character reference is opaque too, so the non-breaking space it encodes does not refuse the span.
-- input.jira --
{{&nbsp;a}}
-- rendered.html --
<p><tt>&nbsp;a</tt></p>
-- want.md --
` a`
-- warnings.json --
[]
