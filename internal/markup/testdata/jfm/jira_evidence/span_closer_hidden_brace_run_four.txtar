source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A run of four braces after a hidden closer leaves no closer behind either; jiro reads the same forced newline between the literal braces.
-- input.jira --
{{a\\}}}}
-- rendered.html --
<p>{{a<br class="atl-forced-newline" />}}}}</p>
-- want.md --
{{a\
}}}}
-- warnings.json --
[]
