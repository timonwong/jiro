source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: A backslash pair ending the image source is a forced newline before the closing `!`, so Jira reads no image; jiro keeps the run as text.
-- input.jira --
!http://x/i\\!
-- rendered.html --
<p>!http://x/i<br class="atl-forced-newline" />!</p>
-- want.md --
!http://x/i\\!
-- warnings.json --
[]
