source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A double hyphen at the end of the body has no following space, so Jira leaves it alone.
-- input.jira --
{{a --}}
-- rendered.html --
<p><tt>a --</tt></p>
-- want.md --
`a --`
-- warnings.json --
[]
