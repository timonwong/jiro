source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A third brace is not a word rune, so the span closes at the first `}}` and the extra brace stays text.
-- input.jira --
{{a}}}
-- rendered.html --
<p><tt>a</tt>}</p>
-- want.md --
`a`}
-- warnings.json --
[]
