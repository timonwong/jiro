source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: The backslash pair is the whole body and hides the closer, so no span forms and the pair renders as a forced newline between literal braces; jiro refuses the span and reads the same break.
-- input.jira --
{{\\}}
-- rendered.html --
<p>{{<br class="atl-forced-newline" />}}</p>
-- want.md --
{{\
}}
-- warnings.json --
[]
