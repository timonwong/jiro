source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira renders the pair as a forced newline and refuses this span; jiro keeps the legacy backslash decode and emits the backslash as inline code.
-- input.jira --
{{\\}}
-- rendered.html --
<p>{{<br class="atl-forced-newline" />}}</p>
-- want.md --
`\`
-- warnings.json --
[]
