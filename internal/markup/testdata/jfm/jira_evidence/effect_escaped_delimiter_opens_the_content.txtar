source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The escaped asterisk is content rather than a candidate, so the bold closes on the last asterisk; jiro reads one bold effect holding the escaped character.
-- input.jira --
*\*b*
-- rendered.html --
<p><b>&#42;b</b></p>
-- want.md --
**\*b**
-- warnings.json --
[]
