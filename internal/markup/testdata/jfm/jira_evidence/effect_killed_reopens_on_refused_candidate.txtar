source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: The one rune of content is a euro sign, so the opener is killed and the refused candidate itself opens the bold Jira renders; jiro reads a literal asterisk, a euro sign and a bold effect.
-- input.jira --
*€*b*
-- rendered.html --
<p>*€<b>b</b></p>
-- want.md --
\*€**b**
-- warnings.json --
[]
