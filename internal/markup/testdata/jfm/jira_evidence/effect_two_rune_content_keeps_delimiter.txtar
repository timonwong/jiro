source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: Two runes of content, one of them multi-byte, let the scan continue, so Jira bolds across the refused candidate; jiro reads one bold effect holding the asterisk.
-- input.jira --
*a€*b*
-- rendered.html --
<p><b>a€*b</b></p>
-- want.md --
**a€\*b**
-- warnings.json --
[]
