source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A single backslash still closes the span at the first `}}` and vanishes, leaving the extra brace as text.
-- input.jira --
{{a\}}}
-- rendered.html --
<p><tt>a</tt>}</p>
-- want.md --
`a`}
-- warnings.json --
[]
