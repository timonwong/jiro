source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A word rune after the closer blocks the citation exactly as it blocks an Effect Delimiter, so jiro stays silent.
-- input.jira --
{{??x??b}}
-- rendered.html --
<p><tt>??x??b</tt></p>
-- want.md --
`??x??b`
-- warnings.json --
[]
