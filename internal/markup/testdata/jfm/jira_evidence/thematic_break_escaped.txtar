source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: One backslash keeps the rule off the line and Jira shows the dashes, so a backslash before the first dash is what protects a dash run jiro writes as text.
-- input.jira --
\----
-- rendered.html --
<p>&#45;---</p>
-- want.md --
\----
-- warnings.json --
[]
