source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A tab written as a character reference is opaque, so the span still forms.
-- input.jira --
{{a&#9;b}}
-- rendered.html --
<p><tt>a&#9;b</tt></p>
-- want.md --
`a	b`
-- warnings.json --
[]
