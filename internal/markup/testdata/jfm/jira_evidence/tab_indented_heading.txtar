source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: A leading tab is skipped before `h1.` too, and jiro converts the heading the same way.
-- input.jira --
	h1. x
-- rendered.html --
<h1><a name="x"></a>x</h1>
-- want.md --
# x
-- warnings.json --
[]
