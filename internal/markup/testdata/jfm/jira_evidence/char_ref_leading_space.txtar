source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A leading space written as a character reference is opaque, so the span still forms.
-- input.jira --
{{&#32;a}}
-- rendered.html --
<p><tt>&#32;a</tt></p>
-- want.md --
`  a `
-- warnings.json --
[]
