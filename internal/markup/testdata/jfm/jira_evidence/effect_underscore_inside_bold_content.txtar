source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A word-attached underscore opens no italic, so Jira bolds the whole content; jiro reads one bold effect whose content is plain text.
-- input.jira --
*a_b*
-- rendered.html --
<p><b>a_b</b></p>
-- want.md --
**a\_b**
-- warnings.json --
[]
