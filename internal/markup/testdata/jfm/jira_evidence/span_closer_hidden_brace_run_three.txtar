source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A hidden closer takes the whole `}` run with it, so the third brace closes nothing and no span forms.
-- input.jira --
{{a\\}}}
-- rendered.html --
<p>{{a<br class="atl-forced-newline" />}}}</p>
-- want.md --
{{a\\}}}
-- warnings.json --
[]
