source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: The trailing space refuses the Monospace Span, and Jira substitutes an en dash in the literal text that remains.
-- input.jira --
{{x -- }}
-- rendered.html --
<p>{{x &#8211; }}</p>
-- want.md --
{{x -- }}
-- warnings.json --
[]
