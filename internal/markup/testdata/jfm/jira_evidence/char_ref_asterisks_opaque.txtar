source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Character references stay opaque, so no effect pair forms and jiro stays silent.
-- input.jira --
{{&#42;x&#42;}}
-- rendered.html --
<p><tt>&#42;x&#42;</tt></p>
-- want.md --
`*x*`
-- warnings.json --
[]
