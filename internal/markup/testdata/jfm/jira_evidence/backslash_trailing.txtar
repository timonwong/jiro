source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: The span closes at the first `}}` and the single backslash in front of it is consumed, so the code body is the text Jira shows.
-- input.jira --
{{a\}}
-- rendered.html --
<p><tt>a</tt></p>
-- want.md --
`a`
-- warnings.json --
[]
