source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A title ending in a backslash still forms a link when one space follows it, and the trim removes that space again, so the rendered title ends in the backslash; this is how jiro writes such a title.
-- input.jira --
[x|http://x|t\ ]
-- rendered.html --
<p><a href="http://x" title="t\" class="external-link" target="_blank" rel="nofollow noopener">x</a></p>
-- want.md --
[x](http://x "t\\")
-- warnings.json --
[]
