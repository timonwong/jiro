source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: Escaping the closing parenthesis alone is enough to stop the token, so Jira shows its characters.
-- input.jira --
(x\)
-- rendered.html --
<p>(x&#41;</p>
-- want.md --
(x)
-- warnings.json --
[]
