source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A heading level Jira does have ends the paragraph above it, with no space after the `.` as well.
-- input.jira --
a
h1.x
-- rendered.html --
<p>a</p>
<h1><a name="x"></a>x</h1>
-- want.md --
a

# x
-- warnings.json --
[]
