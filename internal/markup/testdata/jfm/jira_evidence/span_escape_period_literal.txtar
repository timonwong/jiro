source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: `.` is not escapable inside a span either; the backslash stays visible and conversion back keeps both characters.
-- input.jira --
{{a\.b}}
-- rendered.html --
<p><tt>a\.b</tt></p>
-- want.md --
`a\.b`
-- warnings.json --
[]
