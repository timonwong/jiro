source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira refuses a Monospace Span whose body carries U+200B strictly inside it; jiro keeps the run literal and preserves the rune.
-- input.jira --
{{a​b}}
-- rendered.html --
<p>{{a​b}}</p>
-- want.md --
{{a​b}}
-- warnings.json --
[]
