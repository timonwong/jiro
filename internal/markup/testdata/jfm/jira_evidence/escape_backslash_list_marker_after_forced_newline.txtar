source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-23
observed: An escaped marker on the line after a forced newline keeps that line inside the paragraph — the unprotected probe is in round7 of hack/jira-render-evidence.py.
-- input.jira --
x\\
\* item
-- rendered.html --
<p>x<br class="atl-forced-newline" />
&#42; item</p>
-- want.md --
x\
\* item
-- warnings.json --
[]
