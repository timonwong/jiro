source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira does not autolink a bare email address, so jiro stays silent.
-- input.jira --
{{a@b.c}}
-- rendered.html --
<p><tt>a@b.c</tt></p>
-- want.md --
`a@b.c`
-- warnings.json --
[]
