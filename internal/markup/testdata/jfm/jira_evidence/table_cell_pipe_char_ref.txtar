source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: A pipe written as a character reference does not split the cell, so the span forms around it.
-- input.jira --
||h||
|{{a&#124;b}}|
-- rendered.html --
<div class='table-wrap'>
<table class='confluenceTable'><tbody>
<tr>
<th class='confluenceTh'>h</th>
</tr>
<tr>
<td class='confluenceTd'><tt>a&#124;b</tt></td>
</tr>
</tbody></table>
</div>
-- want.md --
| h |
| --- |
| `a\|b` |
-- warnings.json --
[]
