source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: A following word rune suppresses the flag token, so Jira renders its characters.
-- input.jira --
(flag)off
-- rendered.html --
<p>(flag)off</p>
-- want.md --
(flag)off
-- warnings.json --
[]
