source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-24
observed: U+200B ends the body once the closer consumes the backslash, which is the boundary Jira already expects, so the span still forms.
-- input.jira --
{{a​\}}
-- rendered.html --
<p><tt>a​</tt></p>
-- want.md --
`a​`
-- warnings.json --
[]
