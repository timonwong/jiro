source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: As the body's first rune U+200B is only the boundary Jira already expects, so the span still forms.
-- input.jira --
{{​a}}
-- rendered.html --
<p><tt>​a</tt></p>
-- want.md --
`​a`
-- warnings.json --
[]
