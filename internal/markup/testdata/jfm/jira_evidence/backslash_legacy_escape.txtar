source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira decodes the legacy backslash escape inside the span; jiro decodes it the same way.
-- input.jira --
{{x\-y}}
-- rendered.html --
<p><tt>x&#45;y</tt></p>
-- want.md --
`x-y`
-- warnings.json --
[]
