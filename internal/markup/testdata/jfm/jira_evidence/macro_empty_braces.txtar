source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira consumes the empty macro before the Monospace Span forms; jiro keeps the braces in the code body, without a warning because macro consumption is outside the Text Effect, citation, link and text-changing autolink set this conversion reports.
-- input.jira --
{{{}x}}
-- rendered.html --
<p><tt>x</tt></p>
-- want.md --
`{}x`
-- warnings.json --
[]
