source: ASF Jira Server 8.20.10, POST /rest/api/1.0/render (hack/jira-render-evidence.py), captured 2026-08-22
observed: Jira replaces the token with a grey flag icon, so the flag token list needs the longer form too.
-- input.jira --
{{(flagoff)}}
-- rendered.html --
<p><tt><img class="emoticon" src="/jira/images/icons/emoticons/flag_grey.png" height="16" width="16" align="absmiddle" alt="" border="0"/></tt></p>
-- want.md --
`(flagoff)`
-- warnings.json --
[]
